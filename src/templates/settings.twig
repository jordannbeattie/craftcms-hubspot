{% import '_includes/forms' as forms %}

{{ forms.autosuggestField({
  label: 'API Key',
  name: 'hsApiKey',
  value: settings['hsApiKey'],
  required: true,
  autofocus: true,
  suggestEnvVars: true
}) }}
